import { Input } from '@nextui-org/react';

const TourImagesForm = () => {
  return (
    <Input
      type="file"
      label="Tour Main Cover Image"
      labelPlacement="outside"
      placeholder="Add Tour Main Cover Image"
    />
  );
};

export default TourImagesForm;
